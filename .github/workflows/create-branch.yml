name: Setup Branch

on:
  create:
    branches: "*"

jobs:
  setup-branch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: HEY
        run: |
          echo "HEY"
